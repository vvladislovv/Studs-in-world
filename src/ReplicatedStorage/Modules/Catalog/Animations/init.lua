return {
	Climb = require(script.Climb),
	Fall = require(script.Fall),
	Idle = require(script.Idle),
	Jump = require(script.Jump),
	Run = require(script.Run),
	Swim = require(script.Swim),
	Walk = require(script.Walk),
}